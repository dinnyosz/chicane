# Required — Slack credentials
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token

# Optional — Default working directory for Claude sessions
# BASE_DIRECTORY=/Users/you/projects

# Optional — Comma-separated Slack user IDs that can use the bot
# ALLOWED_USERS=U0123456789,U9876543210

# Optional — Whitelist channels to auto-map to directories under BASE_DIRECTORY
# Simple: channel name = directory name (e.g. #magaldi → BASE_DIRECTORY/magaldi)
# CHANNEL_DIRS=magaldi,slack-bot,frontend
# Custom mapping: channel=path (relative to BASE_DIRECTORY or absolute)
# CHANNEL_DIRS=magaldi,web=frontend,infra=/opt/infrastructure

# Optional — Claude model override (default: whatever claude CLI uses)
# CLAUDE_MODEL=sonnet

# Optional — Permission mode for Claude CLI (default, bypassPermissions, etc.)
# CLAUDE_PERMISSION_MODE=default

# Optional — Debug logging
# DEBUG=true
